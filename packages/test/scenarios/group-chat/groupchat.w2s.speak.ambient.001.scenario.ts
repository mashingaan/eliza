/**
 * Generated by _generate.ts from duke-trust-lab/When2Speak (CC BY 4.0).
 * Do not hand-edit; regenerate with `bun packages/test/scenarios/group-chat/_generate.ts`.
 */
import { scenario } from "@elizaos/scenario-runner/schema";
import {
  buildGroupChatTimingSetup,
  type GroupChatTimingScenarioConfig,
} from "./_factory.ts";

const config = {
  id: "groupchat.w2s.speak.ambient.001",
  title: "Group chat timing: SPEAK (ambient address, 2 speakers)",
  label: "speak",
  directlyAddressed: false,
  context: [
    {
      speaker: "Speaker_0",
      text: "So about the rent deduction for self-employed, I was reading that we can deduct the full cost of rent from our gross profit when we report our weekly income What do you think",
    },
  ],
  decisionTurn: {
    speaker: "Speaker_1",
    text: "I think that might not be entirely accurate from what I understand we can deduct the rent of places or equipment we use specifically for business purposes But if you're talking about your home, then it's only the portion used for business",
  },
  referenceIntervention:
    "That is correct, Speaker_1 You can deduct the rent for real estate or equipment only if it's used specifically for conducting your business The rent for your personal residence isn't typically deductible, unless part of it is used as your business office",
  sourceRow:
    "When2Speak finetune_test_dialogue.jsonl@092e40995896b0c278a1e32954297ef125b70112 row 179",
} satisfies GroupChatTimingScenarioConfig;
const setup = buildGroupChatTimingSetup(config);

export default scenario({
  lane: "live-only",
  id: "groupchat.w2s.speak.ambient.001",
  title: "Group chat timing: SPEAK (ambient address, 2 speakers)",
  domain: "group-chat",
  ...setup,
  turns: [
    {
      ...setup.decisionTurn,
      assertResponse(text: string) {
        const responseLength = text.trim().length;
        if (responseLength === 0) {
          return "label is SPEAK — an apt intervention was expected here, got no reply";
        }
      },
    },
  ],
  finalChecks: [
    {
      type: "judgeRubric",
      name: "timing:speak",
      minimumScore: 0.7,
      rubric:
        "This is a group chat between human participants, and the final message is a moment where an assistant participant should genuinely intervene. Score whether the reply is a concise, substantive, on-topic contribution that answers the open question, supplies the missing fact, or synthesizes the thread. A corpus reference intervention supplies gold flavor, not required wording: \"That is correct, Speaker_1 You can deduct the rent for real estate or equipment only if it's used specifically for conducting your business The rent for your personal residence isn't typically deductible, unless part of it is used as your business office\"",
    },
  ],
});
