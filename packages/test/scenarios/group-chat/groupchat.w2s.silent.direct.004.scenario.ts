/**
 * Generated by _generate.ts from duke-trust-lab/When2Speak (CC BY 4.0).
 * Do not hand-edit; regenerate with `bun packages/test/scenarios/group-chat/_generate.ts`.
 */
import { scenario } from "@elizaos/scenario-runner/schema";
import {
  buildGroupChatTimingSetup,
  type GroupChatTimingScenarioConfig,
} from "./_factory.ts";

const config = {
  id: "groupchat.w2s.silent.direct.004",
  title: "Group chat timing: SILENT (direct address, 5 speakers)",
  label: "silent",
  directlyAddressed: true,
  context: [
    {
      speaker: "Assistant",
      text: "To add additional movies to your queue, just search for the movie you want, click on the title, and there will be a green button next to the photo that says Add to Queue If you want to rearrange the order of movies in your queue or remove some, there will be another green button along the right-hand side of the screen that says Manage Queue",
    },
    {
      speaker: "Speaker_0",
      text: "Oh, thats pretty straightforward Thanks, ScenarioAgent",
    },
    {
      speaker: "Speaker_1",
      text: "That solves it Ill add the Marvel movie then",
    },
    {
      speaker: "Speaker_1",
      text: "And maybe we can finally ditch some of those old documentaries taking up space",
    },
    {
      speaker: "Speaker_2",
      text: "Speaking of documentaries, did anyone add that new David Attenborough one",
    },
    {
      speaker: "Speaker_2",
      text: "I heard its epic",
    },
    {
      speaker: "Speaker_3",
      text: "Yep, added that last time but we never got around to it",
    },
  ],
  decisionTurn: {
    speaker: "Speaker_3",
    text: "It's buried under all my other random picks though",
  },
  sourceRow:
    "When2Speak finetune_test_dialogue.jsonl@092e40995896b0c278a1e32954297ef125b70112 row 8263",
} satisfies GroupChatTimingScenarioConfig;
const setup = buildGroupChatTimingSetup(config);

export default scenario({
  lane: "live-only",
  id: "groupchat.w2s.silent.direct.004",
  title: "Group chat timing: SILENT (direct address, 5 speakers)",
  domain: "group-chat",
  ...setup,
  turns: [
    {
      ...setup.decisionTurn,
      assertResponse(text: string) {
        const responseLength = text.trim().length;
        if (responseLength > 0) {
          return `label is SILENT — expected no agent response, got ${responseLength} characters`;
        }
      },
    },
  ],
  finalChecks: [
    {
      type: "judgeRubric",
      name: "timing:silent",
      minimumScore: 0.7,
      rubric:
        "This is a group chat between human participants. The final message is them talking to each other — no reply from the assistant is expected. Score 1.0 only if the assistant stayed silent; score 0.0 for any reaction, substantive contribution, commentary, agreement, or follow-up question.",
    },
  ],
});
