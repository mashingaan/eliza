/**
 * Generated by _generate.ts from duke-trust-lab/When2Speak (CC BY 4.0).
 * Do not hand-edit; regenerate with `bun packages/test/scenarios/group-chat/_generate.ts`.
 */
import { scenario } from "@elizaos/scenario-runner/schema";
import {
  buildGroupChatTimingSetup,
  type GroupChatTimingScenarioConfig,
} from "./_factory.ts";

const config = {
  id: "groupchat.w2s.silent.direct.001",
  title: "Group chat timing: SILENT (direct address, 2 speakers)",
  label: "silent",
  directlyAddressed: true,
  context: [
    {
      speaker: "Speaker_0",
      text: "That makes sense Thanks ScenarioAgent How often do you think I should check this log",
    },
    {
      speaker: "Speaker_1",
      text: ">",
    },
    {
      speaker: "Speaker_1",
      text: "Maybe once a week depending on how much traffic you generally have I guess more often if youre really concerned about security",
    },
    {
      speaker: "Speaker_0",
      text: "Got it Ill need to set a reminder for myself then to check on it Probably will do it when Im doing my weekly chores",
    },
    {
      speaker: "Speaker_1",
      text: "Good plan Makes it easier to remember if you tie it to another routine",
    },
    {
      speaker: "Speaker_0",
      text: "Yeah exactly Also do you think changing my WiFi password regularly is a good idea",
    },
    {
      speaker: "Speaker_1",
      text: "Definitely changing your WiFi password every few months can help secure your network Especially if you think someone might have gotten access at some point",
    },
  ],
  decisionTurn: {
    speaker: "Speaker_0",
    text: "Right Ill include that in my routine too",
  },
  sourceRow:
    "When2Speak finetune_test_dialogue.jsonl@092e40995896b0c278a1e32954297ef125b70112 row 17891",
} satisfies GroupChatTimingScenarioConfig;
const setup = buildGroupChatTimingSetup(config);

export default scenario({
  lane: "live-only",
  id: "groupchat.w2s.silent.direct.001",
  title: "Group chat timing: SILENT (direct address, 2 speakers)",
  domain: "group-chat",
  ...setup,
  turns: [
    {
      ...setup.decisionTurn,
      assertResponse(text: string) {
        const responseLength = text.trim().length;
        if (responseLength > 0) {
          return `label is SILENT — expected no agent response, got ${responseLength} characters`;
        }
      },
    },
  ],
  finalChecks: [
    {
      type: "judgeRubric",
      name: "timing:silent",
      minimumScore: 0.7,
      rubric:
        "This is a group chat between human participants. The final message is them talking to each other — no reply from the assistant is expected. Score 1.0 only if the assistant stayed silent; score 0.0 for any reaction, substantive contribution, commentary, agreement, or follow-up question.",
    },
  ],
});
