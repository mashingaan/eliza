/**
 * Generated by _generate.ts from duke-trust-lab/When2Speak (CC BY 4.0).
 * Do not hand-edit; regenerate with `bun packages/test/scenarios/group-chat/_generate.ts`.
 */
import { scenario } from "@elizaos/scenario-runner/schema";
import {
  buildGroupChatTimingSetup,
  type GroupChatTimingScenarioConfig,
} from "./_factory.ts";

const config = {
  id: "groupchat.w2s.silent.direct.007",
  title: "Group chat timing: SILENT (direct address, 2 speakers)",
  label: "silent",
  directlyAddressed: true,
  context: [
    {
      speaker: "Speaker_1",
      text: "That's an interesting perspective ScenarioAgent So youre suggesting that the platform itself could act as a leader in a sense if it resonates enough",
    },
    {
      speaker: "Speaker_0",
      text: "I guess that could shift the dynamics rather than waiting for a leader to define the platform the platform could define the leaders",
    },
    {
      speaker: "Speaker_1",
      text: "I like that a lot It turns the usual equation on its head Plus it might allow us to really focus on the issues that matter instead of getting caught up in personality politics",
    },
    {
      speaker: "Speaker_0",
      text: "Exactly This could be our chance to really highlight pressing issues like climate change or economic inequality and let those priorities shape our leadership",
    },
    {
      speaker: "Speaker_1",
      text: "And if we get it right we could really mobilize the base engage new voters It's about creating a narrative that people can believe in and rally behind",
    },
    {
      speaker: "Speaker_0",
      text: "True we need a narrative that's compelling enough to break through the noise Something bold and affirming",
    },
    {
      speaker: "Speaker_1",
      text: "So possibly our next step should be drafting some key points What do we absolutely want to include in our platform What are our non-negotiables",
    },
  ],
  decisionTurn: {
    speaker: "Speaker_0",
    text: "Health care for sure We cant back down on that it's too critical especially given the current debates",
  },
  sourceRow:
    "When2Speak finetune_test_dialogue.jsonl@092e40995896b0c278a1e32954297ef125b70112 row 17988",
} satisfies GroupChatTimingScenarioConfig;
const setup = buildGroupChatTimingSetup(config);

export default scenario({
  lane: "live-only",
  id: "groupchat.w2s.silent.direct.007",
  title: "Group chat timing: SILENT (direct address, 2 speakers)",
  domain: "group-chat",
  ...setup,
  turns: [
    {
      ...setup.decisionTurn,
      assertResponse(text: string) {
        const responseLength = text.trim().length;
        if (responseLength > 0) {
          return `label is SILENT — expected no agent response, got ${responseLength} characters`;
        }
      },
    },
  ],
  finalChecks: [
    {
      type: "judgeRubric",
      name: "timing:silent",
      minimumScore: 0.7,
      rubric:
        "This is a group chat between human participants. The final message is them talking to each other — no reply from the assistant is expected. Score 1.0 only if the assistant stayed silent; score 0.0 for any reaction, substantive contribution, commentary, agreement, or follow-up question.",
    },
  ],
});
