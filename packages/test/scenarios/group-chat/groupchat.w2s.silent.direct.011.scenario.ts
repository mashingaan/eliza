/**
 * Generated by _generate.ts from duke-trust-lab/When2Speak (CC BY 4.0).
 * Do not hand-edit; regenerate with `bun packages/test/scenarios/group-chat/_generate.ts`.
 */
import { scenario } from "@elizaos/scenario-runner/schema";
import {
  buildGroupChatTimingSetup,
  type GroupChatTimingScenarioConfig,
} from "./_factory.ts";

const config = {
  id: "groupchat.w2s.silent.direct.011",
  title: "Group chat timing: SILENT (direct address, 6 speakers)",
  label: "silent",
  directlyAddressed: true,
  context: [
    {
      speaker: "Speaker_3",
      text: "But how does that conversion actually happen Ive always found it a bit like magic",
    },
    {
      speaker: "Speaker_4",
      text: "Ah, I think it involves magnets and coils somehow The spinning somehow generates electricity",
    },
    {
      speaker: "Assistant",
      text: "Actually, to clarify, dynamos operate on the principle of electromagnetic induction A permanent magnet is rotated by a crank This spinning magnet is positioned so that its north and south poles passed by a piece of iron wrapped with wire It was discovered that the spinning magnet produced a pulse of current in the wire each time a pole passed the coil Moreover, the north and south poles of the magnet induce currents in opposite directions By adding a commutator, it is possible to convert the alternating current to direct current, making the energy more usable",
    },
    {
      speaker: "Speaker_0",
      text: "Hm, I thought it was simpler than that Sounds like theres quite a bit of engineering involved",
    },
    {
      speaker: "Speaker_1",
      text: "Wait, but I heard that modern dynamos use electromagnets instead of permanent magnets Wouldn't that make the process different",
    },
    {
      speaker: "Speaker_2",
      text: "Yeah, I second that question ScenarioAgent, isnt the use of electromagnets more efficient here",
    },
    {
      speaker: "Assistant",
      text: "While it is true that some modern generators use electromagnets due to their stronger magnetic fields, the underlying principle remains the same Electromagnets can be controlled more precisely, which can improve efficiency However, this requires an initial electrical input to create the magnetic field, different from the self-powered nature of a basic dynamo with a permanent magnet",
    },
  ],
  decisionTurn: {
    speaker: "Speaker_0",
    text: "So, you need power to make power That's kinda ironic, isnt it",
  },
  sourceRow:
    "When2Speak finetune_test_dialogue.jsonl@092e40995896b0c278a1e32954297ef125b70112 row 5376",
} satisfies GroupChatTimingScenarioConfig;
const setup = buildGroupChatTimingSetup(config);

export default scenario({
  lane: "live-only",
  id: "groupchat.w2s.silent.direct.011",
  title: "Group chat timing: SILENT (direct address, 6 speakers)",
  domain: "group-chat",
  ...setup,
  turns: [
    {
      ...setup.decisionTurn,
      assertResponse(text: string) {
        const responseLength = text.trim().length;
        if (responseLength > 0) {
          return `label is SILENT — expected no agent response, got ${responseLength} characters`;
        }
      },
    },
  ],
  finalChecks: [
    {
      type: "judgeRubric",
      name: "timing:silent",
      minimumScore: 0.7,
      rubric:
        "This is a group chat between human participants. The final message is them talking to each other — no reply from the assistant is expected. Score 1.0 only if the assistant stayed silent; score 0.0 for any reaction, substantive contribution, commentary, agreement, or follow-up question.",
    },
  ],
});
