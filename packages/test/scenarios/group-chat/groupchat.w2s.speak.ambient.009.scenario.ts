/**
 * Generated by _generate.ts from duke-trust-lab/When2Speak (CC BY 4.0).
 * Do not hand-edit; regenerate with `bun packages/test/scenarios/group-chat/_generate.ts`.
 */
import { scenario } from "@elizaos/scenario-runner/schema";
import {
  buildGroupChatTimingSetup,
  type GroupChatTimingScenarioConfig,
} from "./_factory.ts";

const config = {
  id: "groupchat.w2s.speak.ambient.009",
  title: "Group chat timing: SPEAK (ambient address, 3 speakers)",
  label: "speak",
  directlyAddressed: false,
  context: [
    {
      speaker: "Speaker_0",
      text: "Where did the late medieval writer and humanist Giovanni Boccacio live",
    },
    {
      speaker: "Speaker_1",
      text: "If I recall correctly he was associated with the Italian Renaissance but I am not entirely sure of the city Was it Florence",
    },
  ],
  decisionTurn: {
    speaker: "Speaker_2",
    text: "Yes Florence sounds right because didnt he set The Decameron there Itd make sense for him to be in close proximity to his setting",
  },
  referenceIntervention:
    "Actually Giovanni Boccacio indeed lived in Florence which is where he set his famed work The Decameron Florence was a cultural and commercial hub during the Renaissance making it an ideal setting for his stories",
  sourceRow:
    "When2Speak finetune_test_dialogue.jsonl@092e40995896b0c278a1e32954297ef125b70112 row 3091",
} satisfies GroupChatTimingScenarioConfig;
const setup = buildGroupChatTimingSetup(config);

export default scenario({
  lane: "live-only",
  id: "groupchat.w2s.speak.ambient.009",
  title: "Group chat timing: SPEAK (ambient address, 3 speakers)",
  domain: "group-chat",
  ...setup,
  turns: [
    {
      ...setup.decisionTurn,
      assertResponse(text: string) {
        const responseLength = text.trim().length;
        if (responseLength === 0) {
          return "label is SPEAK — an apt intervention was expected here, got no reply";
        }
      },
    },
  ],
  finalChecks: [
    {
      type: "judgeRubric",
      name: "timing:speak",
      minimumScore: 0.7,
      rubric:
        'This is a group chat between human participants, and the final message is a moment where an assistant participant should genuinely intervene. Score whether the reply is a concise, substantive, on-topic contribution that answers the open question, supplies the missing fact, or synthesizes the thread. A corpus reference intervention supplies gold flavor, not required wording: "Actually Giovanni Boccacio indeed lived in Florence which is where he set his famed work The Decameron Florence was a cultural and commercial hub during the Renaissance making it an ideal setting for his stories"',
    },
  ],
});
