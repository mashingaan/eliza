/**
 * Generated by _generate.ts from duke-trust-lab/When2Speak (CC BY 4.0).
 * Do not hand-edit; regenerate with `bun packages/test/scenarios/group-chat/_generate.ts`.
 */
import { scenario } from "@elizaos/scenario-runner/schema";
import {
  buildGroupChatTimingSetup,
  type GroupChatTimingScenarioConfig,
} from "./_factory.ts";

const config = {
  id: "groupchat.w2s.silent.ambient.005",
  title: "Group chat timing: SILENT (ambient address, 5 speakers)",
  label: "silent",
  directlyAddressed: false,
  context: [
    {
      speaker: "Speaker_2",
      text: "But then there's always the chance they simply chose to leave it out to promote exclusive releases later It's a common marketing tactic",
    },
    {
      speaker: "Speaker_3",
      text: "I've seen companies do that They release a basic version first then trickle out special editions with extra content",
    },
    {
      speaker: "Speaker_0",
      text: "Do you know where I might find the song then if it's not on the official soundtrack",
    },
    {
      speaker: "Assistant",
      text: "The song Times a Wasting by Joaquin Phoenix and Reese Witherspoon from the movie Walk the Line is not listed on the official soundtrack However it can be found on other music platforms and occasionally in cover versions by other artists which might not be sanctioned officially",
    },
    {
      speaker: "Speaker_1",
      text: "Thanks",
    },
    {
      speaker: "Speaker_2",
      text: "I've also heard about people using music sharing services to get a hold of such tracks",
    },
    {
      speaker: "Speaker_3",
      text: "True though that raises issues about copyright and the legality of downloading tracks from unofficial sources",
    },
  ],
  decisionTurn: {
    speaker: "Speaker_0",
    text: "It's a bit of a hassle just to listen to one song legally",
  },
  sourceRow:
    "When2Speak finetune_test_dialogue.jsonl@092e40995896b0c278a1e32954297ef125b70112 row 14412",
} satisfies GroupChatTimingScenarioConfig;
const setup = buildGroupChatTimingSetup(config);

export default scenario({
  lane: "live-only",
  id: "groupchat.w2s.silent.ambient.005",
  title: "Group chat timing: SILENT (ambient address, 5 speakers)",
  domain: "group-chat",
  ...setup,
  turns: [
    {
      ...setup.decisionTurn,
      assertResponse(text: string) {
        const responseLength = text.trim().length;
        if (responseLength > 0) {
          return `label is SILENT — expected no agent response, got ${responseLength} characters`;
        }
      },
    },
  ],
  finalChecks: [
    {
      type: "judgeRubric",
      name: "timing:silent",
      minimumScore: 0.7,
      rubric:
        "This is a group chat between human participants. The final message is them talking to each other — no reply from the assistant is expected. Score 1.0 only if the assistant stayed silent; score 0.0 for any reaction, substantive contribution, commentary, agreement, or follow-up question.",
    },
  ],
});
